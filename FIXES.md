# تدقيق موقع teqaniglass.com — UI/UX و SEO

المصدر: كود الصفحة الرئيسية (index.php) بتاريخ 2026-08-26
القالب: HTML/PHP يدوي على Bootstrap 5 — ليس ووردبريس.

> **مهم:** لوحة `/admin` تدير على الأرجح صور الخدمات والأعمال فقط.
> معظم التعديلات أدناه تتطلب تحرير `index.php` عبر **مدير الملفات في cPanel** أو **FTP**.
> **خذ نسخة احتياطية من `index.php` قبل أي تعديل.**

---

# 🔴 المستوى صفر — أعطال تُفقدك عملاء الآن

هذه ليست تحسينات. هذه أعطال. أصلحها اليوم.

## 1. البريد الإلكتروني وهمي — في مكانين

```
قسم "تواصل معنا":  info@gmail.com     ← ليس بريداً حقيقياً
الفوتر:            info@gmial.com     ← خطأ إملائي (gmial) فوق ذلك
```

`info@gmail.com` عنوان غير موجود يملكه شخص آخر. أي عميل يراسلك عليه **يضيع**.
والاختلاف بين النسختين يضر بالسيو المحلي (تناسق بيانات NAP).

**الإصلاح:** استبدل الاثنين ببريد حقيقي على نطاقك، مثل `info@teqaniglass.com`
(يمكنك إنشاؤه مجاناً من cPanel ← Email Accounts).

## 2. نموذج التواصل معطّل — كل رسالة تُرسل تضيع

```html
<form action="" method="post" role="form" class="php-email-form">
```
و في نهاية الصفحة:
```html
<!-- <script src="assets/vendor/php-email-form/validate.js"></script> -->
```

الـ `action` فارغ، وسكربت التحقق/الإرسال **معطّل بالتعليق**. النتيجة: الضغط على
"إرسال الرسالة" يعيد تحميل الصفحة فقط، والرسالة لا تصل لأحد.

**الإصلاح الأسرع والأضمن:** استبدل النموذج بزر واتساب مباشر — معدّل الاستجابة
في السوق السعودي أعلى بكثير من نماذج البريد أصلاً:

```html
<a href="https://wa.me/966556166509?text=السلام%20عليكم،%20أرغب%20بالاستفسار%20عن%20خدمات%20تركيب%20الزجاج"
   target="_blank" rel="noopener" class="btn-get-started">
   تواصل عبر واتساب
</a>
```

**أو** أصلح النموذج فعلياً: `action="contact.php"` مع ملف PHP يرسل بـ `mail()`،
وأزل التعليق عن `validate.js`. لا تتركه كما هو.

## 3. كل روابط الفوتر والتواصل الاجتماعي ميتة

```html
<a href="#" class="twitter">   <a href="#" class="facebook">
<a href="#" class="instagram"> <a href="#" class="linkedin">
<li><a href="#">الرئيسية</a></li>   ... وكل روابط "الصفحات" و"من خدماتنا"
```

**14 رابطاً** كلها `href="#"`. تُشعر الزائر بأن الموقع مهجور.

**الإصلاح:** ضع الروابط الحقيقية، **واحذف أيقونة أي منصة لا تملك فيها حساباً**.
حساب غير موجود أسوأ من عدم عرض الأيقونة. روابط الفوتر الداخلية اجعلها
`href="#about"` و `href="#team"` وهكذا لتطابق القائمة العلوية.

## 4. الخريطة تشير إلى وسط الدمام — لا إلى مؤسستك

الـ iframe يحمل إحداثيات مدينة الدمام العامة، لا موقع المؤسسة.

**الإصلاح:** خرائط جوجل ← ابحث عن نشاطك ← مشاركة ← تضمين خريطة ← انسخ الكود.
وأضف `loading="lazy"` (راجع بند 14).

---

# 🟠 المستوى الأول — سيو: أعلى عائد مقابل الجهد

## 5. لا توجد بيانات منظّمة (Schema) — أكبر مكسب متاح لك

لا يوجد أي JSON-LD في الصفحة. هذا ما يجعل جوجل يعرض ساعات العمل والتقييم
والهاتف والخريطة مباشرة في نتائج البحث.

**الإصلاح:** الملف جاهز في `patches/02-schema-localbusiness.html` — املأ البيانات
وضعه قبل `</body>`. ثم تحقق منه على https://validator.schema.org

## 6. رقم الجوال داخل عنوان الصفحة

```html
<title>زجاج سكريت في الدمام 0556166509</title>
```

جوجل يعرض ~60 حرفاً فقط من العنوان. الرقم يستهلك 10 منها بلا أي فائدة بحثية —
لا أحد يبحث عن "0556166509". وجوجل غالباً يعيد كتابة مثل هذه العناوين.

**البديل:** `زجاج سكريت في الدمام والخبر | مؤسسة التقنية للزجاج`
أضاف "الخبر" و اسم العلامة في نفس المساحة. الرقم مكانه الصحيح في الـ schema
وفي زر الاتصال الثابت.

## 7. `<meta name="keywords">` — 40 كلمة مفتاحية

جوجل **توقف عن استخدام هذا الوسم منذ 2009**. لا ينفع إطلاقاً، وبهذا الحجم
(مع تكرار "الزجاج السكريت" مرتين، ورقم الجوال ككلمة مفتاحية) قد يُقرأ كإشارة سبام.

**الإصلاح:** احذف السطر بالكامل.

## 8. لا يوجد رابط معياري (canonical)

موقعك متاح على `/` و `/index.php` و `www.` و بدونها — أربع نسخ لنفس الصفحة
في نظر جوجل، وقوة الروابط تتشتت بينها.

**الإصلاح:** `<link rel="canonical" href="https://teqaniglass.com/">` (موجود في الـ head الجاهز).

## 9. وسوم المشاركة (Open Graph) مكسورة

```html
<meta property="og:site_name" content="index.php"/>      ← اسم الموقع "index.php"!
<meta name="twitter:site" content="index.php">           ← يفترض أن يكون @حساب
<meta property="og:image" content="assets/img/logo.png"> ← رابط نسبي: لا يعمل
<meta property="og:type" content="article" />            ← مكرر ومتناقض مع "website" أدناه
```

عملياً: عند مشاركة الموقع في واتساب لا تظهر صورة، ويظهر الاسم "index.php".

**الإصلاح:** الـ head الجاهز في `patches/01-head.html` يصلح هذا كله.
تحتاج فقط تجهيز صورة **أفقية 1200×630** باسم `assets/img/og-cover.jpg`
(الشعار المربع لا يصلح لهذا الغرض).

## 10. لا يوجد robots.txt ولا sitemap.xml

جاهزان في `patches/` — ارفعهما إلى جذر الموقع.
`robots.txt` يمنع أيضاً فهرسة `/admin` في نتائج البحث.

## 11. ملف النشاط في خرائط جوجل (Google Business Profile)

**بالنسبة لنشاط خدمي محلي مثل نشاطك، هذا أهم من الموقع نفسه.**
معظم عملاء "زجاج سكريت الدمام" يأتون من خرائط جوجل لا من نتائج الويب.

إن لم يكن لديك ملف: أنشئه على https://business.google.com — مجاناً.
إن كان لديك: أضف صور الأعمال الحقيقية، ساعات العمل، واطلب تقييمات من عملائك.

## 12. رقم الهاتف بصيغة محلية

`tel:0556166509` لا يعمل للمتصلين من خارج السعودية.
**الإصلاح:** `tel:+966556166509` في كل المواضع (4 مواضع في الكود).

---

# 🟡 المستوى الثاني — السرعة

السرعة عامل ترتيب مباشر في جوجل، وتؤثر على معدل المغادرة.

## 13. صفحتك تُحمّل ~40 صورة دفعة واحدة

عدّ الصور: 8 خدمات + 28 عمل + 5 آراء + hero + about + الشعار = **44 صورة**.
وكلها بأسماء مثل `WhatsApp Image 2023-07-09 at 11.32.33 AM.jpeg` — أي صور
واتساب/كاميرا **غير مضغوطة**، وكل واحدة قد تتجاوز 2 ميجابايت.

هذه على الأرجح أكبر مشكلة تقنية في الموقع. الصفحة قد تتجاوز **40 ميجابايت**.

**الإصلاح — بالترتيب:**

**أ) التحميل الكسول** — الأسرع تطبيقاً وأكبر أثراً فورياً.
أضف `loading="lazy"` لكل صورة **عدا صورة الـ hero** (الأولى يجب أن تبقى فورية):
```html
<img src="admin/img/works/IMG-20230226-WA0009.jpg" loading="lazy" decoding="async" class="img-fluid" alt="...">
```

**ب) ضغط الصور** — نزّل الصور، مرّرها على https://squoosh.app أو
https://tinypng.com، وأعد رفعها. استهدف **أقل من 200KB** للصورة الواحدة
بعرض أقصاه 1600 بكسل. هذا وحده قد يقلص الصفحة بنسبة 90%.

**ج) أبعاد ثابتة** — أضف `width` و `height` لكل صورة. بدونها تقفز الصفحة
أثناء التحميل (مقياس CLS في جوجل يعاقب هذا).

**د) أسماء الملفات** — `WhatsApp Image 2023-07-09 at 11.32.33 AM.jpeg` سيء لسببين:
المسافات غير مُرمّزة في الروابط (قد تنكسر على بعض الخوادم)، ولا تحمل أي دلالة
لبحث الصور. أعد التسمية إلى:
```
kabinat-shower-zujaj-dammam-01.jpg
wagihat-zujajiya-dammam-01.jpg
darabzin-zujaj-alkhobar-01.jpg
```

## 14. مكتبتا أيقونات كاملتان لأجل ~15 أيقونة

```html
<link href="assets/vendor/bootstrap-icons/bootstrap-icons.css" ...>
<link href="assets/vendor/boxicons/css/boxicons.min.css" ...>
```

المكتبتان معاً تتجاوزان **500 كيلوبايت** من ملفات الخطوط. أنت تستخدم من
Boxicons ست أيقونات فقط: `bx-fingerprint` `bx-gift` `bx-atom` `bx-chevron-left`
`bxs-quote-alt-left/right` `bxl-*`.

**الإصلاح:** احذف boxicons واستبدل استخداماتها بما يقابلها في Bootstrap Icons
(`bi-fingerprint` `bi-gift` `bi-stars` `bi-chevron-left` `bi-quote` `bi-twitter-x`…).

## 15. خريطة جوجل تُحمّل فوراً

الـ iframe يجلب مئات الكيلوبايتات من سكربتات جوجل عند فتح الصفحة، رغم أن
الخريطة في أسفل الصفحة ولا يراها معظم الزوار.

**الإصلاح:** أضف `loading="lazy"` و `title` للـ iframe:
```html
<iframe loading="lazy" title="موقع مؤسسة التقنية للزجاج في الدمام" style="border:0;width:100%;height:270px" src="..."></iframe>
```

## 16. مكتبات JS مُحمّلة بلا استخدام

- **glightbox** — محمّل (CSS + JS) لكن **لا يوجد أي عنصر مربوط به**.
  إما احذفه، أو — الأفضل — فعّله فعلياً (راجع بند 21).
- **AOS** — القالب يستخدم `data-aos="fade-up"` في قسم الآراء، لكن مكتبة AOS
  غير محمّلة أصلاً. السمة ميتة. احذفها أو حمّل المكتبة.

---

# 🟢 المستوى الثالث — بنية الموقع (أكبر مكسب سيو على المدى الطويل)

## 17. الموقع كله صفحة واحدة — وهذا سقف نموّك

كل روابط التنقل مجرد مراسي داخلية: `#hero` `#about` `#team` `#portfolio` `#contact`.
هذا يعني أن لديك **صفحة واحدة قابلة للفهرسة في كامل الموقع**.

النتيجة العملية: أنت تنافس بصفحة واحدة على عشرات الكلمات المفتاحية معاً، فلا
تتصدر أياً منها. صفحة واحدة لا يمكنها التركيز على "كبائن شاور الخبر"
و "درابزين زجاج القطيف" و "واجهات محلات الدمام" في آن واحد.

**الإصلاح — وهو الأكبر أثراً في هذه الوثيقة:** أنشئ صفحة مستقلة لكل خدمة رئيسية،
لكل واحدة عنوانها ووصفها و H1 و schema خاص بها:

| الصفحة | العنوان المقترح |
|---|---|
| `/wagihat-zujajiya-dammam` | تركيب واجهات زجاجية للمحلات في الدمام \| مؤسسة التقنية |
| `/darabzin-zujaj-dammam` | تركيب درابزين زجاج وستانلس في الدمام \| مؤسسة التقنية |
| `/kabaen-shower-dammam` | تركيب كبائن شاور زجاجية في الدمام \| مؤسسة التقنية |
| `/abwab-zujaj-securit-dammam` | تركيب أبواب زجاج سكريت في الدمام \| مؤسسة التقنية |
| `/sianat-abwab-zujajiya-dammam` | صيانة الأبواب الزجاجية في الدمام — خدمة طوارئ |
| `/zujaj-securit-alkhobar` | زجاج سكريت في الخبر \| مؤسسة التقنية |

كل صفحة تحتاج **600–800 كلمة محتوى حقيقي**: الأنواع، السماكات، الأسعار
التقريبية، مدة التنفيذ، الضمان، صور أعمال من نفس النوع، وأسئلة شائعة.

⚠️ **لا تنسخ نفس النص وتبدّل اسم المدينة فقط.** جوجل يصنّف ذلك محتوى مكرراً
منخفض القيمة (Doorway Pages) وقد يعاقب عليه. كل صفحة تحتاج محتوى يخصّها فعلاً.

---

# 🔵 المستوى الرابع — واجهة المستخدم والثقة

## 18. اتجاه الصفحة RTL غير معرّف

```html
<html lang="ar">          ← ينقصه dir="rtl"
```

⚠️ **هذا البند تعامل معه بحذر.** القالب أصلاً إنجليزي وتم قلبه يدوياً في
`style.css` (الدليل: استخدام `bx-chevron-left` للقوائم العربية). إضافة
`dir="rtl"` قد **تقلب التصميم مرتين فينكسر**.

**الطريقة الصحيحة:** جرّبها على نسخة اختبارية أولاً. إن انكسر التصميم، أبقِ
الوضع الحالي — الفائدة لا تستحق كسر الموقع. الأثر الحقيقي لغيابها يظهر فقط
في الأرقام والنصوص المختلطة عربي/إنجليزي.

## 19. الشعار يتصل بالهاتف بدلاً من العودة للرئيسية

```html
<a href="tel:0556166509" class="logo"><img src="assets/img/logo.png" alt=""></a>
```

الضغط على الشعار **يبدأ مكالمة**. هذا يخالف توقّع كل مستخدم على الإنترنت —
الشعار يعود للرئيسية دائماً. زائر يضغط بالخطأ فيجد هاتفه يتصل: تجربة سيئة.

**الإصلاح:**
```html
<a href="/" class="logo"><img src="assets/img/logo.png" alt="مؤسسة التقنية للزجاج السكريت — الدمام" class="img-fluid"></a>
```
(لاحظ: `alt` كان فارغاً — والشعار من أهم الصور التي تحتاج نصاً بديلاً.)

## 20. الأرقام في قسم الإحصائيات تضر بالمصداقية

```
232 عميل   |   521 مشروع   |   125 موظف   |   24 ساعة
```

**"125 موظف"** لمؤسسة زجاج محلية رقم غير مصدّق، ويجعل الزائر يشك في بقية
ما تقوله الصفحة. كما أن عدّاد "24" تحت عنوان "العمل على مدار 24 ساعة" ليس
إحصائية أصلاً.

**الإصلاح:** أرقام حقيقية أفضل. `"15 سنة خبرة"` و `"+500 مشروع منجز"`
و `"ضمان سنتين"` أقوى إقناعاً من رقم مبالغ فيه.

## 21. قسم "آراء العملاء" لا يحتوي أي رأي عميل

قسم `testimonials` يعرض خمس شرائح فيها **وصف خدمات**، بلا اسم عميل واحد،
ولا صورة عميل، ولا تقييم.

هذا يهدر أقوى عنصر إقناع في الصفحة. **الدليل الاجتماعي هو ما يحسم قرار
العميل في الخدمات المنزلية.**

**الإصلاح:** استبدلها بتقييمات حقيقية — اسم العميل، حيّه أو مدينته، الخدمة
المنفّذة، ونص التقييم. الأفضل: لقطات من تقييمات خرائط جوجل الحقيقية.

## 22. صور الأعمال لا تُفتح بحجم أكبر

معرض الأعمال (~28 صورة) لا يمكن تكبير أي صورة فيه. لنشاط **بصري بالكامل**
مثل تركيب الزجاج، الزائر يريد رؤية التفاصيل والتشطيب عن قرب.

المفارقة: مكتبة **glightbox محمّلة بالفعل** لكن غير مربوطة بشيء.

**الإصلاح:** لفّ كل صورة برابط:
```html
<a href="admin/img/works/IMG-20230226-WA0009.jpg" class="glightbox" data-gallery="works">
  <img src="admin/img/works/IMG-20230226-WA0009.jpg" loading="lazy" class="img-fluid" alt="تركيب زجاج ومرايا في الدمام">
</a>
```
وأضف في `assets/js/main.js`: `GLightbox({ selector: '.glightbox' });`

## 23. النصوص البديلة (alt) — فارغة أو مكررة

**مشكلة أ — سمة `alt` مكررة على نفس الوسم** (في كل بطاقات الخدمات الثمانية):
```html
<img src="..." alt="زجاج سكريت في الدمام" class="img-fluid" alt="">
                 ↑ الأولى                              ↑ والثانية فارغة!
```
HTML غير صالح. المتصفح يأخذ الأولى، لكن يجب حذف الثانية.

**مشكلة ب — نص واحد مكرر 8 مرات:** كل صور الخدمات تحمل `alt="زجاج سكريت في الدمام"`.
هذا حشو كلمات مفتاحية، وعديم الفائدة لقارئ الشاشة.

**مشكلة ج — ~33 صورة بـ `alt=""`** في المعرض والآراء والـ hero.

**الإصلاح:** نص بديل وصفي فريد لكل صورة يصف **ما في الصورة فعلاً**:
```html
alt="واجهة زجاجية سيكوريت لمحل تجاري في الدمام"
alt="درابزين زجاج مع قائم ستانلس ستيل لدرج فيلا"
alt="كابينة شاور زجاج شفاف بمقبض ستانلس"
```

## 24. أخطاء HTML تكسر بنية العناوين

**أ) وسم `<h5>` داخل `<h2>` وغير مُغلق** — في قسم الـ hero:
```html
<h2>
  <h5><strong>مؤسسة التقنية للزجاج السكريت </strong>نقدم خدمات... </h2>
     ↑ فُتح h5 ولم يُغلق أبداً، و h2 يُغلق بدلاً منه
```
**الإصلاح:**
```html
<p class="hero-sub"><strong>مؤسسة التقنية للزجاج السكريت</strong> نقدم خدمات تركيب الزجاج السكريت والواجهات الزجاجية والأبواب والدرابزين في الدمام والخبر بأعلى جودة وأسعار منافسة</p>
```

**ب) علامة `<` زائدة** — في قسم الآراء:
```html
<h3>تركيب زجاج سكريت في السعودية<</h3>
                                 ↑ احذفها
```

## 25. رقم جوال المطوّر منشور في الكود

```html
<meta name="author" content=" Ramzy Ali // +966535069717 ">
```
رقم شخصي ظاهر في مصدر الصفحة لأي زائر أو روبوت جمع بيانات.
**الإصلاح:** `<meta name="author" content="مؤسسة التقنية للزجاج السكريت">`
(رابط المطوّر في الفوتر يكفي للأمانة الأدبية.)

## 26. صور مكرّرة بتصنيفات متناقضة

`IMG-20230226-WA0009.jpg` معروضة كـ **"تركيب أبواب زجاج"** في الخدمات،
وكـ **"زجاج ومرايا"** في المعرض.
`IMG-20230226-WA0012.jpg` معروضة كـ **"كبائن شاور"** و **"شاور"** في مكانين.

كما أن أزرار الفلترة تقول "كبائن شاور" بينما عنوان الصور تحتها "شاور".

**الإصلاح:** وحّد التسميات، ولا تكرّر نفس الصورة في تصنيفين مختلفين.

## 27. لا يوجد حماية من السبام في النموذج

النموذج (إن أُصلح) بلا كابتشا ولا حقل خادع. ستصلك رسائل سبام آلية.
**الإصلاح:** أضف حقلاً مخفياً (honeypot) — إن امتلأ، تجاهل الرسالة.

## 28. `/admin` مكشوف للعموم

لوحة التحكم متاحة لأي شخص يعرف الرابط، وصور الموقع تُقدَّم من داخل
مجلد الإدارة (`admin/img/...`).

**الإصلاح:**
1. `robots.txt` الجاهز يمنع فهرستها (لكنه لا يمنع الوصول).
2. للحماية الفعلية: cPanel ← **Directory Privacy** ← كلمة مرور إضافية على `/admin`.
3. تأكد أن كلمة مرور اللوحة قوية وليست `admin` أو `123456`.
4. تأكد أن رفع الملفات في اللوحة **يقبل الصور فقط** — رفع ملف `.php` عبر
   لوحة غير محمية هو أخطر ثغرة ممكنة في موقع كهذا.

---

# خطة التنفيذ المقترحة

| المرحلة | البنود | الوقت | الأثر |
|---|---|---|---|
| **اليوم** | 1، 2، 3، 12 — البريد، النموذج، الروابط الميتة، صيغة الهاتف | ساعة | يوقف نزيف العملاء فوراً |
| **هذا الأسبوع** | 5–10 — الـ head الجديد + schema + robots + sitemap | ساعتان | أساس السيو كاملاً |
| **هذا الأسبوع** | 11 — ملف خرائط جوجل | ساعة | الأعلى أثراً لنشاط محلي |
| **الأسبوع القادم** | 13، 15 — ضغط الصور + lazy loading | 3 ساعات | السرعة تتضاعف |
| **الأسبوع القادم** | 19–24 — إصلاحات الواجهة والثقة | 3 ساعات | معدل التحويل |
| **الشهر القادم** | 17 — صفحات الخدمات المستقلة | مشروع | نمو الترتيب فعلياً |

**بعد رفع التعديلات:** سجّل الموقع في https://search.google.com/search-console
وأرسل `sitemap.xml`، ثم قِس السرعة على https://pagespeed.web.dev

---

## الملفات الجاهزة في `patches/`

| الملف | ماذا تفعل به |
|---|---|
| `01-head.html` | استبدل به كامل محتوى `<head>` في `index.php` |
| `02-schema-localbusiness.html` | املأ بياناتك، ثم ضعه قبل `</body>` |
| `robots.txt` | ارفعه إلى جذر الموقع |
| `sitemap.xml` | ارفعه إلى جذر الموقع |

⚠️ الملفان الأولان يحتويان حقولاً بين `<<< >>>` — **يجب ملؤها ببياناتك الحقيقية
قبل الرفع.** لا تترك أي قيمة تخمينية، خصوصاً في الـ schema.
